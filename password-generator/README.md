### PASSWORD GENERATOR

This is a password generator made by react using useEffect , useCallBack to make the method more optimized and useRef to make the copy feature

If you want to run it locally then clone this repo go into that folder and to install all the dependencies do "npm install" and then to start the server do "npm run dev"