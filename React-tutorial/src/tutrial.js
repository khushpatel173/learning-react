// Tutorial of react from hitesh - chai aur code

// the name of the components should start with the upper case

// when you return something from an fn in react it should be wrapped in one tag like only one tag will be returned , so we generally written the whole thing in a in a empty tag <> </> we call this fragment

// if in jsx we need to use or access any cariables we use {} these are called expressions or evaluated expressions

// useState is used to change the state of variable mtlb to change variable value in the whole document

// virtual dom - rather than pura dom firse load kro we only load those jisme kuch ui changes hue ho vo hm track krte he and sirf usse hi firse load krte he 

// new react architecture use fibre architecture which basically stops for a few moment and if many updates come together then they send it together or liek setting priority and stuff reconsilliation is the algorith which was reffered as virtual DOM  which is basically a diff algorithm like github
 
//  in jsx names like class and all are reserved keywords so we use liek className and alternatives like that like htmlFor and stuff like that
  
// lets say we have a card component which we are importing and we need diff text in all of them for that we use props

// props- we can send them when we are writing like <Card special='asdfasdf'/>  like this and this can be access when we call the function which returns this as an argument

// useState 